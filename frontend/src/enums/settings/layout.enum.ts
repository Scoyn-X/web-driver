/**
 * 菜单布局枚举
 */
export const enum LayoutMode {
  /** 左侧菜单布局 */
  LEFT = "left",
  /** 顶部菜单布局 */
  TOP = "top",
  /** 混合菜单布局 */
  MIX = "mix",
}

/**
 * 侧边栏状态枚举
 */
export const enum SidebarStatus {
  /** 展开 */
  OPENED = "opened",
  /** 关闭 */
  CLOSED = "closed",
}

/**
 * 组件尺寸枚举
 */
export const enum ComponentSize {
  DEFAULT = "default",
  LARGE = "large",
  SMALL = "small",
}
